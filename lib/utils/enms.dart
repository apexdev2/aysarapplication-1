enum AppLanguages { ar, en }

enum AppGender { male, female }

enum ApiRequestType {
  get,
  post,
  delete,
}

enum LoginMethod { mobile, email }

enum LauncherType { link, mobile, email ,sms,whatsapp}

enum Membershipstypes {
  type1,
  type2,
  type3,
}

enum CacheKeys {
  firstVisit,
  profileImage,
  loggedIn,
  remember,
  language,
  theme,
  userToken,
  fcmToken,
  appSignature,
  name,
  mobile,
  email,
  splashBg,
  isActive,
  activeBiometric,
  countryCode,
  dialCode,
  userType
}
enum TosterTypes{
  warning,
  sucsses,
  failed
}